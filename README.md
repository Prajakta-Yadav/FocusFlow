# FocusFlow
ocusFlow is a Pomodoro timer web app built with HTML, CSS, and JavaScript. It uses a 25-minute countdown implemented with setInterval() and a timer() function. Features include Start, Pause, and Reset controls, plus an audio alarm and alert notification when the session completes.
